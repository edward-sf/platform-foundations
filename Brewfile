# Local toolchain for platform-foundations. Install with: brew bundle
# Terraform comes from hashicorp/tap (already installed); CI pins 1.16.4.
# Not from Homebrew (versions match CI; `make doctor` checks they are on PATH):
#   bicep  0.47.16: az bicep install --version v0.47.16 && ln -sf ~/.azure/bin/bicep ~/.local/bin/bicep
#   tflint 0.64.0:  release zip from github.com/terraform-linters/tflint, checked against checksums.txt, into ~/.local/bin
brew "azure-cli"
brew "pre-commit"
brew "actionlint"
brew "zizmor"
brew "shellcheck"
brew "gitleaks"
brew "bats-core"
brew "jq"
brew "gh"
